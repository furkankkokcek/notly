Create a new component with the name: $ARGUMENTS

Steps:

1. Create `js/components/$ARGUMENTS.js` with this starter:
```js
function render$ARGUMENTS() {
  // TODO: implement
}
```

2. Create `css/components/$ARGUMENTS.css` with this starter:
```css
/* $ARGUMENTS component styles */
```

3. In `index.html`, add (in the correct order):
   - `<link rel="stylesheet" href="css/components/$ARGUMENTS.css">` in `<head>` after the last CSS `<link>`
   - `<script src="js/components/$ARGUMENTS.js"></script>` before `<script src="pages/home.js">`

4. Confirm the files were created and the load order in `index.html` is correct.
